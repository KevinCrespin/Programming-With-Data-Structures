package bst;

import bst.BST.TreeNode;

public class BSTDelete <E extends Comparable<E>>{

	/** Delete an element from the binary tree.
	 * Return true if the element is deleted successfully
	 * Return false if the element is not in the tree */
	public boolean delete(E e, TreeNode<E> root) {
		// Locate the node to be deleted and also locate its parent node
        // I set this to return false so that the code would compile.  To do: make this work correctly
	
		return false; // Whether Element deleted successfully
	}

	

}